# Getting started

Run the following:
```sh
pipx install uv
git clone github.com/FHETCH/march-demo.git
cd march-demo
pip install .
```

# Development

This repository is developed using `uv` as the package manager.
